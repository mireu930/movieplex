<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.movie.plex.like.ContentsLikeDAO">
	
	<!-- 좋아요 여부 확인 -->
	<select id="movieLikeCheck" parameterType="ContentsLikeDTO" resultType="Long">
			SELECT COUNT(*) FROM CONTENTSLIKE
			WHERE USERNUM= #{userNum} 
			AND CONTENTID = #{contentId}
			AND KIND =0
	</select>
	
	<!-- 좋아요 추가 -->
	<insert id="movieAddLike" parameterType="ContentsLikeDTO">
			INSERT INTO CONTENTSLIKE (LIKEID, USERNUM, CONTENTID, KIND)
			VALUES (LIKEID_SEQ.NEXTVAL,#{userNum},#{contentId},0)
	</insert>
	
	<!-- 좋아요 취소 -->
	<delete id="movieDeleteLike" parameterType="ContentsLikeDTO">
			DELETE FROM CONTENTSLIKE
			WHERE USERNUM= #{userNum}
			AND CONTENTID = #{contentId}
			AND KIND = 0
	</delete>
	
	
	
	
	
	
	
	
	
	
  </mapper>